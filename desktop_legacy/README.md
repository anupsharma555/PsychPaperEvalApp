# Desktop Legacy (Tk)

This directory contains the deprecated Tk desktop client.

- Default desktop runtime is now the Tauri shell in `desktop_shell/` with UI in `desktop_ui/`.
- Legacy launcher script `scripts/paper_eval_gui.py` is blocked by default.
- To run legacy mode intentionally, set `PAPER_EVAL_ALLOW_LEGACY_UI=1`.
