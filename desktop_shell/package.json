{
  "name": "papereval-desktop-shell",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac": "tauri build --bundles app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  }
}
