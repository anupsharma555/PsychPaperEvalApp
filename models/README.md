# Models Directory

Place local model artifacts here (for example `.gguf` and multimodal projection files).

These files are intentionally ignored by git to keep repository size manageable.

Expected pattern examples:
- `Qwen*.gguf`
- `mmproj*.gguf`
